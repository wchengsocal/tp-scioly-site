# OS cruft
Thumbs.db
desktop.ini
.DS_Store

# Editor
.vscode/
.idea/

# Local tooling state — not part of the site
.impeccable/
.claude/

# Unused source images kept locally but not shipped
falcon-red-outline-hi.png
falconnew.png

# Full-resolution camera originals of the site photographs. The shipped copies
# in the root are downscaled to 1600px on the long edge; these are the source
# they were resampled from. Kept locally so the resize can be redone, never
# published — they are ~12MB.
_originals/
